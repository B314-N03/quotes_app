import 'package:flutter/material.dart';

class print extends StatelessWidget {
  const print(List favo_quotes_saved, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
